services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      PYTHONPATH: /app/src
    command: ["uv", "run", "uvicorn", "api.server:app", "--host", "0.0.0.0", "--port", "8000"]
